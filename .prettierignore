# Bundled Quran data — kept minified to reduce app bundle size.
# Regenerated by scripts/download-quran.ts (which writes minified JSON).
data/quran-data.json

# Build output
dist/

# Vendored design references — not app code.
design_handoff_*/
